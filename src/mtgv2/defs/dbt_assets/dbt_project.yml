name: mtg

profile: mtg

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_assets:
    staging:
      +materialized: view
      +target: staging
    intermediate:
      +materialized: view
      +target: intermediate
    marts:
      +materialized: table
      +target: marts
flags:
  require_generic_test_arguments_property: true
